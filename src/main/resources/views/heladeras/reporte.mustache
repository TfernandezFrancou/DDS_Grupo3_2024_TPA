<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reporte Falla Técnica</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/estilo-reporte-heladeras.css">
    <link rel="icon" type="image/x-icon" href="/views/imagenes/favicon.ico">
</head>

<body>
    <a href="/heladeras" class="back-button">&lt; Volver</a>
    <div class="container">
        <h1>Reporte Falla Técnica</h1>
        <form action="/heladeras/reporte" method="POST" enctype="multipart/form-data">
            <label for="idHeladera">Heladera <span>*</span></label>
            <select id="idHeladera" name="idHeladera" required>
                <option value="" disabled selected>Seleccione una heladera</option>
                {{#heladeras}}
                    <option value="{{idHeladera}}">{{nombre}}</option>
                {{/heladeras}}
            </select>

            <label for="fechaFalla">Fecha Registro de Falla <span>*</span></label>
            <input type="date" id="fechaFalla" name="fechaFalla" required>

            <label for="fotoAdjunta">Foto (URL)</label>
            <input type="text" id="foto" name="foto">

            <label for="descripcion">Descripcion</label>
            <textarea id="descripcion" name="descripcion" rows="5"></textarea>

            {{#error}}
            <p class="text-danger">{{error}}</p>
            {{/error}}

            <div class="d-flex justify-content-center">
                <button type="submit">Enviar</button>
            </div>
        </form>
    </div>
</body>