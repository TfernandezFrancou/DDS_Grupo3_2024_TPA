<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de Falla</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/detalleFalla.css">
    <link rel="icon" type="image/x-icon" href="/views/imagenes/favicon.ico">
</head>

<body>
<a href="/reportes" class="back-button">&lt; Volver</a>
<div class="container">
    <h1 class="text-center mb-4">Reporte</h1>
    <div class="row">
         <div class="col-md-4">
            <div class="detalle-info">
                <div class="campo mb-3">
                    <label>Nombre de Heladera</label>
                    <div class="valor">{{falla.heladera.nombre}}</div>
                </div>
                <div class="campo mb-3">
                    <label>Fecha de Registro</label>
                    <div class="valor">{{falla.fecha}}</div>
                </div>
                <div class="campo mb-3">
                    <label>Dirección</label>
                    <div class="valor">{{falla.heladera.direccion}}</div>
                </div>
                <div class="campo mb-3">
                    <label>Tipo de Falla</label>
                    <div class="valor">{{falla.tipoDeIncidente}}</div>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            <div class="foto-container">
                <h5 class="text-center mb-3">Foto adjunta</h5>
                <div class="foto">
                    <img src="{{falla.foto}}" alt="Foto de la falla" class="img-fluid rounded shadow">
                </div>
            </div>
        </div>
    </div>
</div>
</body>

</html>
